VB=trigger !< /^Trig/ < VoicePassive < proposing $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were proposing (trigger) to leave (target)