VB=trigger !< /^Trig/ < VoicePassive < design $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were design (trigger) to leave (target)