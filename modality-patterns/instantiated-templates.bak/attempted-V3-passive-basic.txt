VB=trigger !< /^Trig/ < VoicePassive < attempted $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were attempted (trigger) to leave (target)