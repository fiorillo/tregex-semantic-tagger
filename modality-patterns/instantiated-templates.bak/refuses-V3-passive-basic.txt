VB=trigger !< /^Trig/ < VoicePassive < refuses $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigNotPermit) >2 trigger
insert (TargNotPermit) >1 target

% the attackers were refuses (trigger) to leave (target)