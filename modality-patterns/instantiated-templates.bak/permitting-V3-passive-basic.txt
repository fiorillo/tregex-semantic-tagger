VB=trigger !< /^Trig/ < VoicePassive < permitting $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were permitting (trigger) to leave (target)