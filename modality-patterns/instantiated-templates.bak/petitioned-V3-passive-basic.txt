VB=trigger !< /^Trig/ < VoicePassive < petitioned $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigWant) >2 trigger
insert (TargWant) >1 target

% the attackers were petitioned (trigger) to leave (target)