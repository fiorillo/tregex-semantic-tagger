VB=trigger !< /^Trig/ < VoicePassive < designing $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were designing (trigger) to leave (target)