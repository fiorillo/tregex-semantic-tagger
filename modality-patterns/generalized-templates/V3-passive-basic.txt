VB=trigger !< /^Trig/ < VoicePassive < $verb $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (Trig$label) >2 trigger
insert (Targ$label) >1 target

% the attackers were $verb (trigger) to leave (target)
