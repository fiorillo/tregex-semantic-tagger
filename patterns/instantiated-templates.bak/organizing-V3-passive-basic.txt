VB=trigger !< /^Trig/ < VoicePassive < organizing $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were organizing (trigger) to leave (target)