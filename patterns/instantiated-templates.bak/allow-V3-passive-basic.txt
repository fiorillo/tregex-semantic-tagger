VB=trigger !< /^Trig/ < VoicePassive < allow $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were allow (trigger) to leave (target)