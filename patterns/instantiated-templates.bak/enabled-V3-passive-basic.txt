VB=trigger !< /^Trig/ < VoicePassive < enabled $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were enabled (trigger) to leave (target)