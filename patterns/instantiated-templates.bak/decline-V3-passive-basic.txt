VB=trigger !< /^Trig/ < VoicePassive < decline $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigNotEffort) >2 trigger
insert (TargNotEffort) >1 target

% the attackers were decline (trigger) to leave (target)