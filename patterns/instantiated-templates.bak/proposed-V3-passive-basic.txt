VB=trigger !< /^Trig/ < VoicePassive < proposed $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were proposed (trigger) to leave (target)