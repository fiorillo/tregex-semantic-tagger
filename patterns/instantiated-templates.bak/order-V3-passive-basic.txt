VB=trigger !< /^Trig/ < VoicePassive < order $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were order (trigger) to leave (target)