VB=trigger !< /^Trig/ < VoicePassive < organizes $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were organizes (trigger) to leave (target)