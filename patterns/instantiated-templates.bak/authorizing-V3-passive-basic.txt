VB=trigger !< /^Trig/ < VoicePassive < authorizing $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were authorizing (trigger) to leave (target)