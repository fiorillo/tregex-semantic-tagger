VB=trigger !< /^Trig/ < VoicePassive < tries $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were tries (trigger) to leave (target)