VB=trigger !< /^Trig/ < VoicePassive < propose $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were propose (trigger) to leave (target)