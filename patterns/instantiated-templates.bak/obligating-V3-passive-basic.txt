VB=trigger !< /^Trig/ < VoicePassive < obligating $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were obligating (trigger) to leave (target)