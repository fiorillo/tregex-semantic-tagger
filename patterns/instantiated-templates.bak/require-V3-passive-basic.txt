VB=trigger !< /^Trig/ < VoicePassive < require $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were require (trigger) to leave (target)