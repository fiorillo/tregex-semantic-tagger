VB=trigger !< /^Trig/ < VoicePassive < declines $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigNotEffort) >2 trigger
insert (TargNotEffort) >1 target

% the attackers were declines (trigger) to leave (target)
