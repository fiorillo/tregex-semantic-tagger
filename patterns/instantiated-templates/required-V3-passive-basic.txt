VB=trigger !< /^Trig/ < VoicePassive < required $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were required (trigger) to leave (target)
