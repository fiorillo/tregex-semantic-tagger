VB=trigger !< /^Trig/ < VoicePassive < attempts $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were attempts (trigger) to leave (target)
