VB=trigger !< /^Trig/ < VoicePassive < plots $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were plots (trigger) to leave (target)
