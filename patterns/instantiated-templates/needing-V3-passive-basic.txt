VB=trigger !< /^Trig/ < VoicePassive < needing $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were needing (trigger) to leave (target)
