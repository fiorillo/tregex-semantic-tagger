VB=trigger !< /^Trig/ < VoicePassive < planning $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were planning (trigger) to leave (target)
