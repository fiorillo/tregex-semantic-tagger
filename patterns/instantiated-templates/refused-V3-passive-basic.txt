VB=trigger !< /^Trig/ < VoicePassive < refused $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigNotPermit) >2 trigger
insert (TargNotPermit) >1 target

% the attackers were refused (trigger) to leave (target)
