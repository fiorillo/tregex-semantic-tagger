VB=trigger !< /^Trig/ < VoicePassive < intending $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were intending (trigger) to leave (target)
