VP < (VP < (VB=trigger !< /^Trig/ < VoicePassive < avoiding)) $,, (NP=target)

insert (TargNotEffort) >1 target
insert (TrigNotEffort) >2 trigger

% the attacks (target) were avoiding (trigger)
