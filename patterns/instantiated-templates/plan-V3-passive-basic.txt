VB=trigger !< /^Trig/ < VoicePassive < plan $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were plan (trigger) to leave (target)
