VB=trigger !< /^Trig/ < VoicePassive < requests $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigWant) >2 trigger
insert (TargWant) >1 target

% the attackers were requests (trigger) to leave (target)
