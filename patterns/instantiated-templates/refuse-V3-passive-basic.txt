VB=trigger !< /^Trig/ < VoicePassive < refuse $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigNotPermit) >2 trigger
insert (TargNotPermit) >1 target

% the attackers were refuse (trigger) to leave (target)
