VB=trigger !< /^Trig/ < VoicePassive < try $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were try (trigger) to leave (target)
