VB=trigger !< /^Trig/ < VoicePassive < tried $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were tried (trigger) to leave (target)
