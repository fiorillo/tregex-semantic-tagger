VB=trigger !< /^Trig/ < VoicePassive < contriving $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were contriving (trigger) to leave (target)
