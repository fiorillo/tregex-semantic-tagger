VB=trigger !< /^Trig/ < VoicePassive < needs $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were needs (trigger) to leave (target)
