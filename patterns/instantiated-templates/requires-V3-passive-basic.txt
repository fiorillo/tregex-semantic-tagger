VB=trigger !< /^Trig/ < VoicePassive < requires $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were requires (trigger) to leave (target)
