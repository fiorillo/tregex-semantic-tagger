VB=trigger !< /^Trig/ < VoicePassive < entreats $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigWant) >2 trigger
insert (TargWant) >1 target

% the attackers were entreats (trigger) to leave (target)
