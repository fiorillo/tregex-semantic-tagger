VB=trigger !< /^Trig/ < VoicePassive < orders $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were orders (trigger) to leave (target)
