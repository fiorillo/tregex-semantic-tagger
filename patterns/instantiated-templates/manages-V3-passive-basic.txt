VB=trigger !< /^Trig/ < VoicePassive < manages $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigSucceed) >2 trigger
insert (TargSucceed) >1 target

% the attackers were manages (trigger) to leave (target)
