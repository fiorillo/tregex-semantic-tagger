VB=trigger !< /^Trig/ < VoicePassive < petitions $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigWant) >2 trigger
insert (TargWant) >1 target

% the attackers were petitions (trigger) to leave (target)
