VP < (VP < (VB=trigger !< /^Trig/ < VoicePassive < forgoing)) $,, (NP=target)

insert (TargNotEffort) >1 target
insert (TrigNotEffort) >2 trigger

% the attacks (target) were forgoing (trigger)
