VB=trigger !< /^Trig/ < VoicePassive < entreated $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigWant) >2 trigger
insert (TargWant) >1 target

% the attackers were entreated (trigger) to leave (target)
