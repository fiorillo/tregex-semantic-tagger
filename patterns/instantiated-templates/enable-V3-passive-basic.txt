VB=trigger !< /^Trig/ < VoicePassive < enable $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were enable (trigger) to leave (target)