VB=trigger !< /^Trig/ < VoicePassive < plotted $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were plotted (trigger) to leave (target)
