VB=trigger !< /^Trig/ < VoicePassive < proposes $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigIntend) >2 trigger
insert (TargIntend) >1 target

% the attackers were proposes (trigger) to leave (target)
