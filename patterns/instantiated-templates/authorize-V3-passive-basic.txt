VB=trigger !< /^Trig/ < VoicePassive < authorize $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were authorize (trigger) to leave (target)
