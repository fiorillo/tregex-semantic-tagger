VB=trigger !< /^Trig/ < VoicePassive < entreating $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigWant) >2 trigger
insert (TargWant) >1 target

% the attackers were entreating (trigger) to leave (target)
