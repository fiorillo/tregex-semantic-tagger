VB=trigger !< /^Trig/ < VoicePassive < requiring $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were requiring (trigger) to leave (target)
