VB=trigger !< /^Trig/ < VoicePassive < attempting $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were attempting (trigger) to leave (target)
