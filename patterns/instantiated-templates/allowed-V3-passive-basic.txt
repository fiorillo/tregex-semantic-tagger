VB=trigger !< /^Trig/ < VoicePassive < allowed $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigPermit) >2 trigger
insert (TargPermit) >1 target

% the attackers were allowed (trigger) to leave (target)
