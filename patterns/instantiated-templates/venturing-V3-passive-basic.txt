VB=trigger !< /^Trig/ < VoicePassive < venturing $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were venturing (trigger) to leave (target)