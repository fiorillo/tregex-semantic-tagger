VB=trigger !< /^Trig/ < VoicePassive < need $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigRequire) >2 trigger
insert (TargRequire) >1 target

% the attackers were need (trigger) to leave (target)
