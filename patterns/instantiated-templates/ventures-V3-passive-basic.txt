VB=trigger !< /^Trig/ < VoicePassive < ventures $.. (S < (VP < TO < (VP=target < (VB !< AUX))))

insert (TrigEffort) >2 trigger
insert (TargEffort) >1 target

% the attackers were ventures (trigger) to leave (target)